<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cybernetics.hellaecu.linetic.com.service_activity_communication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".UsingBroadCastReceiver.MainActivity"
            android:theme="@style/Base.Theme.AppCompat.Light.DarkActionBar"
            android:label="Broadcast Receiver">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service android:name=".DataGeneratorService" />

        <activity android:name=".UsingEventBus.Main2Activity"
            android:label="Event Bus">

        </activity>
    </application>

</manifest>